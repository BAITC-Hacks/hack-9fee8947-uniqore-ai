# Проверка мобильной шапки

23.09.2026, ветка `fix/mobile-header`, база `f4077ff`.

Исправлена сетка шапки: логотип, AI и справка сверху; загрузка и выгрузка занимают две равные половины второй строки. Все мобильные кнопки имеют высоту 44 px. На 601–760 px убран заголовок продукта, который вытеснял выгрузку за экран. Готовая сборка обновлена.

Проверено на macOS, Node 25.8.1, Python 3.12.1:

- `npm run build` и `npm run format:check` в `frontend/` — успешно.
- `git diff --check` — успешно.
- `python -m moneygraph demo --data data --out out/mobile-header --port 8765` — расчёт и проверка исходного набора успешны, приложение отвечает HTTP 200.
- В браузере на ширинах 320, 390, 600, 601, 760, 768 и 1280 px кнопки шапки не выходят за экран и не имеют внутреннего переполнения. На мобильных ширинах загрузка и выгрузка равны по ширине и высоте.
- Открываются загрузка, подключение AI, справка и меню выгрузки. На 320 px проверена длинная подпись «OpenAI · ключ задан» с вымышленным ключом, без обращения к провайдеру.

Независимый review предложения и итогового CSS не выявил блокирующих замечаний. Изменены только стили и сборка; расчёты, API и данные не затронуты. Проверка на физическом телефоне и пользовательская локальная приёмка ещё ожидаются. Рекомендация: принять после визуальной приёмки; до неё PR не сливать.
