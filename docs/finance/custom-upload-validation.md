# Проверка загрузки собственных данных

Дата: 23.09.2026. Ветка: `feat/custom-data-upload`. Пользователь подтвердил локальную приёмку итоговой версии `2269362` и разрешил слияние 23.09.2026 ([PR #23](https://github.com/BAITC-Hacks/hack-9fee8947-uniqore-ai/pull/23)).

## Версия и среда

Проверенный код после объединения с AI-чатом из `main` (`7b1cddf`): **`b65fd8ff6fe5639645214708d6e97dd4f3113b6f`**. После этого уточнён только текст диалога о сбросе AI-ключа, пересобран frontend, обновлены скриншоты и документация. Новый текст и совместная работа проверены в браузере. Итоговый коммит `226936215ae29889ed1283ad2defa0a1c7281889` принят пользователем. Последующий коммит фиксирует приёмку, перенос карточки в done и объединяет документацию/настройки публикации актуальной main; код приложения и сборка не меняются.

macOS arm64, Python 3.12.1, Node 25.8.1. Использовано существующее Python-окружение с версиями из lock; `python-multipart==0.0.32` установлен отдельно для этой проверки. Свежая установка всех зависимостей, Windows, Linux и удалённый сервер этой итерацией не проверялись. Браузерные сценарии этого отчёта выполнены агентом; последующая пользовательская приёмка зафиксирована выше.

## Автоматические проверки

| Проверка | Результат |
|---|---|
| `python -m pytest -q` | **127 passed**, 18,98 с на объединённой версии |
| `npm test` в `frontend/` | **39 passed**, включая тесты AI-чата с имитацией API |
| `npm run build` | TypeScript и Vite прошли; готовая сборка включена в репозиторий |
| `npm run format:check` | Пройдено |
| `git diff --check` | Пройдено |
| SHA-256 оригиналов | `case.pdf` и `data-description.md` совпадают с исходными суммами |

Два предупреждения Python не являются отказами: переход Starlette TestClient к httpx2 и будущее изменение pandas при смешанных часовых поясах. Такие даты уже отклоняются, что проверено тестом.

Покрыты изоляция двух импортов и исходного снимка, согласованность API/CSV, малые наборы, большие int64 gid, повреждённые файлы, типы, схема, отсутствующие файлы, несогласованные суммы, лимиты объёма/строк/распаковки, очередь импорта, срок хранения и очистка. Отдельный тест отключает HTTP-клиент во время расчёта: результат не занимает слот без получателя токена, временные файлы удалены, блокировка освобождена, следующая загрузка проходит. Уже начавшийся расчёт в потоке завершается; отключение не является мгновенной отменой CPU-работы.

## Полный запуск из исходных Parquet

На чистом коммите `b65fd8f` выполнено:

```bash
python scripts/smoke_local_launch.py --data data --out out/custom-upload-final --port 18971
```

[Машинный отчёт](evidence/custom-upload-smoke.json): **pass**. Новый Python-процесс с импортами, расчётом, записью и проверкой CSV — **4,641 с**; весь smoke — 12,8686 с. Установка исключена, файловый кэш ОС не очищался, память не измерялась. API, досье, локальные объяснения, три CSV и все файлы сборки проверены. Тестовый сервер остановлен, порт освобождён.

Анализ `c138a62d811d6c39`, алгоритм 1.2.0: 2 248 клиентов, 3 119 связей, 4 840 операций, 88 групп, 19 изолированных seed, 444 граничных клиента, 20 строк top. Исторические выгрузки 1.1.0 также прошли verify. `results/`, исходные Parquet и оригиналы условий не изменены.

Первый smoke остановился до запуска из-за занятого порта; чужой процесс сохранён, проверка повторена на свободном порту. Ранний pytest в песочнице блокировал два socket-теста; полный запуск с разрешёнными локальными портами прошёл все 127 тестов.

## Браузерные сценарии

- Пустой комплект: форма показывает отсутствующий файл, запрос не отправляется.
- Повреждённый Parquet: понятная ошибка, исходный анализ и перечень из одного клиента сохранены.
- Четыре синтетических клиента, две связи и две операции за 31.12.2027–01.01.2028: успешный импорт, корректный граф и досье, изолированный seed сохранён, старый перечень сброшен.
- Gid `9007199254741001` показан точно. Выбор 01.01.2028 оставляет одну соответствующую операцию.
- Выгрузка скачала `nodes_roles.csv`; фактический сохранённый файл прочитан, четыре gid совпали с импортом. Инструмент не получил событие download, поэтому сохранение проверено отдельно.
- Перезагрузка вкладки сохраняет импорт. Другая вкладка открывает исходные 2 248 клиентов. Возврат к исходному набору работает.
- После перезапуска сервера старый токен получает 410 с понятным восстановлением; возврат проверен.
- Один день 29.02.2028: верная дата и один столбец активности, ползунки периода отключены.
- После объединения с AI-чатом повторён импорт. Введён вымышленный тестовый ключ без запросов провайдеру. Смена набора сбрасывает ключ; ассистент использует досье нового клиента и предлагает локальные пояснения.
- Экран 1280 px и форма 390×844: элементы читаемы, форма помещается, Escape закрывает диалог. [Результат импорта](screenshots/custom-upload-result.png), [мобильная форма](screenshots/custom-upload-390.png).

## Review и приёмка

Предложение, логические изменения и объединённая версия проверены по протоколу разработки. Замечание о сохранении недоступного результата после отключения клиента исправлено и покрыто тестом; блокирующих замечаний нет. Загрузка не вызывает внешний AI; обязательные функции доступны без ключа и аккаунта.

Рекомендация review: **принять**. Пользовательская локальная приёмка версии `2269362` получена. Ограничения: временное хранение, один серверный процесс, поддерживаемая схема и лимиты. Работа на максимальных размерах и реальный OpenAI-запрос не измерены. Пользователь разрешил слияние; карточка перенесена в [завершённые задачи](../done.md).
