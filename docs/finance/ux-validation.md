# Проверка UX-итерации · 23.09.2026

База сравнения — `73f5230`, ветка — `feat/finance-ux-workspace`. Объём: убрать дублирование навигации и справки, освободить рабочее пространство, сделать фильтры постоянными и объяснить гипотезы ролей. Задача выполняется отдельно от параллельной смены бренда. Перед фиксацией интегрирован актуальный `main` (`10d7c54`): добавлены правила ведения бэклога и документы без изменений приложения; UX-карточка остаётся «В работе» до приёмки. [Исходный отчёт](validation.md) и прежние снимки сохранены без изменений.

Статус: пользователь принял UX-версию `983b2ec` и разрешил слияние 23.09.2026. Последующая интеграция с принятым Tyuin проверена отдельно, см. раздел ниже. Точный commit объединённой версии указан в PR.

## Проверка предложения и реализации

По [протоколу разработки](../development-workflow.md) проведены проверка предложения до реализации и независимое чтение реализованного diff. Рекомендация — принять направление: обязательные роли, сообщества, приоритеты, поиск, граф и три CSV сохраняются. Затронуты соответствие кейсу, реализация, воспроизводимость и понятность демонстрации; баллы по шкалам организатора не присваивались.

До реализации отмечены риски доступности единой справки на узком экране, области действия фильтров и смешения поддержки роли с точностью. При чтении diff отдельно проверены нативный диалог, совпадение `gid` досье и выбранного клиента, правила ролей и выдача подробного объяснения только в досье. Это проверки кода, отдельно от запуска и измерений ниже.

## Автоматическая проверка

- `python -m pytest -q`: **29 passed**, 9,12 с, macOS arm64 / Python 3.12.1. Одно прежнее предупреждение Starlette об адаптере httpx.
- `npm run build` и `npm run format:check`: успешно, TypeScript и форматирование без ошибок.
- `python -m moneygraph verify --data data --out out`: `ok`, 2 248 узлов, 88 сообществ, top-20; снимок `ad6239d9995448b0`. SHA-256 трёх CSV совпали с `results/`.
- `git diff --check`: без ошибок. API-тесты проверяют содержимое всех трёх экспортов и ID снимка.
- Три CSV побайтово совпали с базовой версией: `nodes_roles.csv`, `clusters.csv`, `top_nodes.csv`. Правила ролей, формулы поддержки и приоритета не менялись.
- Дополнительные проверки покрывают соответствие объяснения принятому правилу и формуле балла, ограничения границы/seed/изолятов, точность хранения порогов и отсутствие подробных объяснений в общей выдаче графа и соседях досье.

## Измерение рабочей площади

Размер окна браузера — 1280 × 720 CSS px. Значения сняты с DOM до и после изменения компоновки; это измерение интерфейса, не скорости работы аналитика.

| Область | До, ширина × высота | После, ширина × высота |
|---|---:|---:|
| Вся рабочая область `.investigation` | 1168 × 414,5 | 1248 × 541,5 |
| Очередь | 228 × 412,5 | 268,80 × 539,5 |
| Поле графа | 640 × 206,5 | 606,01 × 311,5 |
| Досье | 298 × 412,5 | 371,20 × 539,5 |

При 1280 × 720 горизонтального и вертикального переполнения документа нет; содержимое панелей прокручивается внутри них. [Снимок рабочего экрана](screenshots/ux-workbench-1280.png).

## Выполненные браузерные сценарии

| Сценарий | Наблюдаемый результат |
|---|---|
| Открыть «Справку и данные», закрыть Escape | Модальный диалог закрыт; фокус возвращён к кнопке справки |
| Глубина 4 | В выборке 444 клиента и 49 сообществ |
| При глубине 4 выбрать сообщество 01 | Глубина сохранена, в выборке 63 узла |
| При глубине 4 включить «Только seed», затем сбросить фильтры | Показано пустое состояние; сброс восстанавливает выборку |
| Ввести `100000000456947100` на вкладке сообществ | Открываются «Приоритеты» и изолированный клиент |
| Гипотеза координатора и раскрытие метода | Показаны наблюдения, пороги, порядок правил, факторы поддержки и ограничения seed; поддержка не названа вероятностью |
| Транзит `100000000031787100` | Поддержка 78/100, отношение выхода к входу 1 при условии 0,8–1,2; входящее окружение содержит 2 узла, полное — 3 |
| Клик по ребру транзитного клиента | Открыта одна операция 30 000 ₸ с источником `c30c5317b543:181` |
| Период 1–1 июля, затем сброс | Пустые операции показаны явно; сброс возвращает месяц |
| Ассистент без ключей | Возвращается подписанное локальное объяснение транзитной гипотезы |
| Экспорт | Три ссылки доступны, скачивание `top_nodes.csv` подтверждено браузерным событием download |
| Поиск → досье → сообщество | Поиск очищен; сводка сообщества подписана «Всего», отфильтрованное число участников показано отдельно |
| Смена клиента / вкладки досье | Прокрутка содержимого возвращается к началу |
| Справка → «Показать клиентов» на границе | Диалог закрывается; фильтр глубины 4, вся сеть, 444 клиента |
| Размеры 1280×720, 1440×900, 1024×768 | Рабочая область помещается в окно, горизонтальной и вертикальной прокрутки документа нет |
| Размер 390×844 | Панели идут друг под другом; горизонтального переполнения нет, справка доступна и прокручивается |

Дополнительные экраны: [1440×900](screenshots/ux-workbench-1440.png), [справка на 390×844](screenshots/ux-help-mobile.png). Изменений исходных Parquet, оригиналов требований и прежних скриншотов нет. Система ролей и численные результаты не менялись; измерение рабочей площади не доказывает экономию времени аналитика. Пользовательская оценка удобства и подтверждение локальной приёмки остаются отдельным этапом.


## Приёмка и интеграция с Tyuin

23.09.2026 пользователь подтвердил результат UX-версии `983b2ec` и поручил слияние PR #12. К этому моменту в `main` (`e8172e0`) вошёл принятый ребрендинг Tyuin. Конфликты разрешены с сохранением постоянных фильтров, просторной компоновки, единой справки и обоснований ролей; сохранены актуальные логотип 1793×877, favicon и цвета Tyuin. Удалённая боковая полоса не восстановлена. Готовые web-артефакты пересобраны из объединённых исходников.

Повторная проверка:

- `python -m pytest -q`: **29 passed**, 9,12 с; прежнее предупреждение Starlette/httpx.
- `npm run build`, `npm run format:check`, `git diff --check`: успешно.
- Аналитика, контракты ролей и тесты идентичны принятому `983b2ec`; изменение сервера при интеграции — только название приложения. Изображения логотипа в source и web совпадают с бренд-пакетом v1.0.2.
- При 1280×720 поле графа сохранило 606,01×311,5 px; документ помещается в окно. Логотип загружен, цвет основной кнопки `#006b75`, боковой полосы нет, справка одна.
- Глубина 4 → 49 сообществ; несовместимый seed-фильтр → пустое состояние; сброс и поиск транзитного клиента → гипотеза с порогами и поддержкой 78/100.
- Справка закрывается Escape и возвращает фокус. При 390×844 логотип виден, горизонтального переполнения нет, справка работает. Ошибок в консоли не обнаружено.
- Независимое чтение разрешённого diff: замечаний к коду нет. Изменения ограничены совмещением двух принятых результатов и фиксацией приёмки; новой функциональности не добавлено.

Новые снимки: [совмещённый интерфейс 1280×720](screenshots/ux-tyuin-workbench-1280.png), [справка 390×844](screenshots/ux-tyuin-help-mobile.png). Прежние снимки сохранены. Задача перенесена в [выполненные](../done.md), принятие нового SHA пользователем отдельно не приписывается.
