# Tyuin на NVIDIA Brev

**Текущая онлайн-версия:** `f4077ff` из `main`, алгоритм 1.2.0, `analysis_id=c138a62d811d6c39`. Передеплой на существующей VM выполнен 23.09.2026; [версия образа, проверки и откат](brev-redeploy-f4077ff.md). Ниже сохранены инструкции и история первоначальных проверок.

Конфигурация запускает существующий CPU-пайплайн и веб-интерфейс в одном контейнере на порту **8765**. GPU, CUDA, NGC и ключ LLM для основного сценария не нужны. Python 3.12, зависимости, готовый frontend и три Parquet из репозитория входят в образ. Первый старт и каждый перезапуск пересчитывают результаты.

## Вариант 1: сборка на Brev VM

Создайте инстанс в [Brev Console](https://brev.nvidia.com/) в **VM Mode**. Выберите CPU-инстанс, если он доступен, либо доступную VM с GPU: приложение GPU не использует. Jupyter для Tyuin не нужен. Docker и Compose должны быть доступны на хосте.

Сначала установите Brev CLI на своём компьютере по [официальной инструкции](https://docs.nvidia.com/brev/cli/getting-started). На Windows используйте WSL с Ubuntu 20.04 или новее: установите CLI внутри WSL и выполняйте локальные команды `brev` в терминале WSL. Команды с `export` и `$(...)` ниже предназначены для Bash; для сборки образа Docker должен быть доступен из того же терминала. Проверьте установку командой `brev --version`.

На своём компьютере подключитесь к VM:

```bash
brev login
brev refresh
brev shell tyuin
```

`tyuin` здесь и ниже — имя вашего инстанса. Для VM Mode используйте обычный SSH-шлюз Brev. В контейнерном режиме для доступа к Docker хоста может понадобиться `--host`. Если этот вариант получает тайм-аут, а обычное SSH-подключение работает и видит Docker, используйте его; именно так выполнен описанный ниже деплой.

Дождитесь `BUILD COMPLETED`, `SHELL READY`, `HEALTH HEALTHY` в `brev ls --json` перед установкой приложения. На VM клонируйте репозиторий и выберите проверенный commit с этим конфигом:

```bash
git clone https://github.com/BAITC-Hacks/hack-9fee8947-uniqore-ai.git
cd hack-9fee8947-uniqore-ai
git checkout <проверенный-commit>
docker compose version
docker compose --env-file .env.example config --quiet
docker compose --env-file .env.example up -d --build --wait --wait-timeout 360
```

Для закрытого репозитория нужна авторизация Git на VM. Не вставляйте токен в URL и не сохраняйте его в конфиге. Образ собирается из текущего checkout; выбранный commit должен уже быть доступен на GitHub. Для первичной сборки нужны Docker Hub и PyPI. Указанные 360 секунд относятся к ожиданию готовности контейнера после сборки.

Проверьте на VM:

```bash
docker compose --env-file .env.example ps
curl --fail http://127.0.0.1:8765/api/health
docker compose --env-file .env.example exec -T app python -m moneygraph verify --data /app/data --out /app/out
```

На своём компьютере откройте SSH-туннель и оставьте команду работающей:

```bash
brev port-forward tyuin --port 8765:8765
```

Откройте [http://127.0.0.1:8765](http://127.0.0.1:8765). Корневой Compose по умолчанию публикует порт только на loopback VM. Если локальный порт занят, используйте `--port 8766:8765` и адрес с портом 8766.

## Вариант 2: Docker Compose в Brev Console / Launchable

Для загрузки в Brev используйте [deploy/brev/docker-compose.yml](../../deploy/brev/docker-compose.yml). Он не зависит от соседнего Dockerfile или checkout: нужен готовый образ в реестре, доступном инстансу.

Сначала соберите образ для Linux amd64 на машине с Docker Buildx и опубликуйте в **разрешённом реестре**. Пример для GHCR; замените `YOUR_ORG` на namespace в нижнем регистре. Авторизуйтесь в реестре стандартным способом перед публикацией.

```bash
export TYUIN_IMAGE="ghcr.io/YOUR_ORG/tyuin:$(git rev-parse --short=12 HEAD)"
docker buildx build --platform linux/amd64 --tag "$TYUIN_IMAGE" --push .
docker compose -f deploy/brev/docker-compose.yml config --quiet
```

Образ содержит конкурсный датасет: сохраняйте доступ в пределах разрешённого использования кейса. Команда `--push` публикует образ; для подготовки локального конфига выполнять её не требуется. Образ должен быть доступен Brev для скачивания; для закрытого реестра предварительно настройте доступ на целевой VM или используйте вариант 1.

В настройках Brev:

| Поле | Значение |
|---|---|
| Software / Runtime | Docker Compose |
| Compose file | Загрузить `deploy/brev/docker-compose.yml` |
| Source | My code files are embedded in my container(s) |
| Jupyter | Выключен |
| Launch parameters | Обязательный Text `TYUIN_IMAGE`: опубликованный тег, лучше `registry/image@sha256:…` |
| Network | Secure Link на порт `8765` |
| View access | Only my organization для командного деплоя |

Brev-конфиг публикует порт 8765 на интерфейсах VM для маршрутизации платформой. Не открывайте сырой TCP-порт всему интернету: используйте **Secure Link** с авторизацией Brev. Приложение не содержит собственной авторизации. Для скриптов и прямых API-запросов используйте SSH-туннель, поскольку Secure Link требует входа через браузер: в VM Mode — `brev port-forward tyuin --port 8765:8765`; в контейнерном режиме для доступа к порту хоста может понадобиться `--host`. Проверенное подключение к инстансу `tyuin` описано [ниже](#подключение-через-ssh).

Оба Compose-файла позволяют изменить адрес и порт хоста через `TYUIN_BIND_ADDRESS` и `TYUIN_PORT`. Для локальной проверки готового образа задайте `TYUIN_BIND_ADDRESS=127.0.0.1`. Внутри контейнера приложение всегда слушает `0.0.0.0:8765`; порт Secure Link должен совпадать с портом хоста.

При обычном создании инстанса без Launchable значения окружения должны быть доступны процессу Compose. Если форма не предлагает Setup values, используйте Launchable с Launch parameters или вариант 1.

## Необязательный AI

Основной расчёт, CSV и блок «Локальные пояснения · без API» работают без ключа. Для чата пользователь вводит свой OpenAI API key во вкладке «Ассистент». Запросы идут непосредственно из браузера в OpenAI; ключ хранится только в памяти страницы и удаляется при перезагрузке. Настраивать серверные переменные для этого чата не нужно. Окно ввода ключа можно закрыть и продолжить работу с локальными пояснениями.

Переменные `LLM_BASE_URL`, `LLM_MODEL`, `LLM_API_KEY` оставлены только для серверного `POST /api/nodes/{gid}/explain`, доступного внешним API-клиентам. Интерфейс ассистента этот endpoint не вызывает. По умолчанию все три переменные пусты, и endpoint возвращает локальное объяснение.

Если внешний провайдер нужен именно этому серверному endpoint, на VM скопируйте `.env.example` в `.env`, внесите значения и запускайте Compose с `--env-file .env`. Compose передаёт значения backend; сам Python файл `.env` не загружает. Не включайте `.env` в Git или образ.

Для того же серверного endpoint в Launchable добавьте три необязательных Text-параметра. Для ключа используйте **Use a secret** и выбранную версию секрета, без значения по умолчанию. Brev передаёт параметры первому запуску Compose; при последующем пересоздании контейнера из SSH их нужно снова предоставить. Перезапуск существующего контейнера сохраняет его окружение. Не выводите полный `docker compose config` или `docker inspect` с настоящими ключами; для валидации достаточно `config --quiet`.

## Управление и данные

Команды выполнять из checkout на VM, с тем же `--env-file`, что при запуске:

```bash
docker compose --env-file .env.example logs --tail=100 app
docker compose --env-file .env.example restart app
docker compose --env-file .env.example exec -T app python -m moneygraph verify --data /app/data --out /app/out
docker compose --env-file .env.example down
```

`down` останавливает приложение и сохраняет том `analysis-output`; `down --volumes` удаляет результаты. При новом старте результаты пересчитываются, а не загружаются вместо анализа. Для обновления выберите новый проверенный commit и повторите `up -d --build --wait --wait-timeout 360`. Для отката выберите предыдущий проверенный commit и пересоберите образ. В режиме готового образа используйте соответствующий сохранённый тег или digest.

Контейнер работает как UID/GID 10001; Docker инициализирует новый именованный том с правами каталога `/app/out`. Не подменяйте его произвольным bind mount без настройки прав. Проверки `/api/health` выполняются и во время начального периода `start-period=300s`: неудачные проверки в этот период не учитываются в лимите повторов. Успешная проверка сразу переводит контейнер в `healthy`, после чего последующие ошибки уже учитываются. Это не задержка запуска проверок на 300 секунд; время ожидания команды Compose отдельно ограничено `--wait-timeout 360`. `restart: unless-stopped` перезапускает завершившийся процесс, но сам по себе не перезапускает контейнер со статусом `unhealthy`.

Остановка контейнера не останавливает облачную VM. Завершение работы и управление стоимостью выполняются отдельно в Brev Console. Локальный Python-запуск из [README](../../README.md) по-прежнему работает без Brev и аккаунтов команды.

## Проверка конфигурации

Проверено 23.09.2026 на Docker Desktop, Engine 25.0.3, Compose 2.24.5: Linux amd64, Python 3.12.14, зависимости без изменений из `requirements.lock`. Код и конфигурация проверены на commit `2a9d54a` поверх `main` `18c412d`; последующее изменение только фиксирует этот отчёт. Локальный ID проверенного образа: `sha256:5636f118f2078249b672b8e965ddba9909ad3eb7ab476e693aff25188dc375db` (это ID локального образа, не опубликованный registry digest).

| Проверка | Результат |
|---|---|
| Оба Compose: `config --quiet` | Успешно; Brev-конфиг без `TYUIN_IMAGE` отклоняется с понятной ошибкой |
| `docker compose --env-file .env.example build` | Образ собран из исходников и существующего lock-файла |
| Корневой Compose: `up --wait` | `healthy`; расчёт из Parquet, запись и проверка результатов завершены |
| Отдельный Compose Brev с `TYUIN_IMAGE=tyuin:local` | `healthy`, без сборки и подключения исходников с хоста |
| `/api/health`, `/api/analysis`, досье и три CSV | Один `analysis_id=ad6239d9995448b0`; 2 248 / 88 / 20 строк, CSV совпадают с файлами контейнера |
| Ассистент без ключа | Локальное объяснение |
| Проверка через браузер | HTTP 200, Tyuin, граф и изображения загружены, ошибок консоли нет |
| Существующие тесты внутри образа без сети | **29 passed**, 28,55 с; одно предупреждение Starlette о будущем переходе TestClient с httpx на httpx2 |
| Перезапуск контейнера и `moneygraph verify` | Повторный `healthy`, 2 248 узлов / 88 кластеров / топ-20, тот же `analysis_id` |
| Содержимое образа и пользователь | UID 10001; `.env`, `.git` и `docs` отсутствуют в `/app` |
| Документация | Локальные ссылки и `git diff --check` проверены |

Проверочные Compose-проекты использовали loopback-порты 18765 и 18766, пустые `LLM_*` и отдельные тома. Команда тестов для Bash из корня репозитория; начиная с PR #17 тестам нужны также `scripts/` и `results/`, которых нет в образе. На `78590c9` она даёт 89 passed ([проверка на чистой машине](../clean-machine-check.md)):

```bash
docker run --rm --network none \
  --mount "type=bind,source=$(pwd)/tests,target=/app/tests,readonly" \
  --mount "type=bind,source=$(pwd)/scripts,target=/app/scripts,readonly" \
  --mount "type=bind,source=$(pwd)/results,target=/app/results,readonly" \
  tyuin:local python -m pytest -q -p no:cacheprovider /app/tests
```

**Review: принять конфигурацию в рамках локально проверенного объёма.** Улучшены запуск и воспроизводимость; неизменные алгоритмы и CSV проверены существующими тестами и запуском. Семантика ролей, ранжирование, точность и масштабируемость этим изменением не улучшались и новых оценок не получают. Для демонстрации появился повторяемый запуск контейнера. Существенных регрессий в затронутых сценариях не найдено.

На этапе этой локальной проверки облачный запуск ещё не выполнялся. Результат последующего деплоя приведён в разделе [«Проверка на Brev»](#проверка-на-brev). Secure Link, публикация в реестр и внешний LLM остаются непроверенными. Задача остаётся в «В работе» до требуемой приёмки; это не разрешение на слияние в `main`.

### Синхронизация с main после review

В ветку PR #16 добавлена `main` `0b5e0ba` merge-коммитом с сохранением истории обеих веток. Единственный конфликт был в README: объединены сведения о проверках macOS, Windows и Docker/Linux. Сохранены инструкции PowerShell, конфигурация Brev, все записи бэклога и настройка `.gitattributes` для точных байтов CSV. Файлы и ветки не удалялись.

Код приложения, зависимости, данные, тесты, Dockerfile и оба Compose не изменились относительно `5383dc4`; результаты предыдущего запуска выше остаются применимы к runtime. При разрешении конфликта проверены diff, отсутствие конфликтных маркеров и удалённых файлов, сохранность изменений обеих сторон, 49 ссылок и синтаксис обоих Compose. Полный runtime-прогон повторно не выполнялся: изменение затрагивает документацию и Git-атрибуты сохранённых CSV, которые не входят в Docker-образ.

## Проверка на Brev

23.09.2026 с разрешения пользователя создан CPU-инстанс `tyuin`, тип по каталогу Brev `cpu-e2.2vcpu-8gb` (Nebius), VM Mode без Jupyter. При создании запрошен диск 50 ГБ. Перед запуском согласованы тариф каталога $0,05952/час за вычисления и $0,12264/ГБ в месяц за диск; это предложение каталога, не измеренный счёт.

Развёрнут commit `3c6b1cd056eda05042870b2034e24f58fb080a16`. Для приватного репозитория использован архив выбранных файлов этого commit: Dockerfile, Compose, `.dockerignore`, `.env.example`, `requirements.lock`, `moneygraph/`, три Parquet и `tests/`. `.git`, рабочий `.env` и GitHub-токен на сервер не передавались. SHA-256 архива до и после передачи совпал: `786bbb78ebe79b9833d1bfd79a4758e4a7ea8693cc84148a1be4b6c07b4c0c40`.

Архив распакован в `~/workspace/tyuin-3c6b1cd`. Из этого каталога выполнены:

```bash
docker compose --env-file .env.example config --quiet
docker compose --env-file .env.example up -d --build --wait --wait-timeout 360
docker compose --env-file .env.example exec -T app python -m moneygraph verify --data /app/data --out /app/out
docker run --rm --network none \
  --mount "type=bind,source=$(pwd)/tests,target=/app/tests,readonly" \
  tyuin:local python -m pytest -q -p no:cacheprovider /app/tests
docker compose --env-file .env.example restart app
docker compose --env-file .env.example up -d --wait --wait-timeout 360
docker compose --env-file .env.example exec -T app python -m moneygraph verify --data /app/data --out /app/out
```

Окружение: Ubuntu 24.04, Linux x86_64, Docker Engine 29.8.1, Compose 5.5.1, Python 3.12.14 в образе. ID образа на VM: `sha256:b0699a9b2ec1a4f506aa839786259b8cad6dcaebb233fe16f78dc4109fee9ac2`; образ не публиковался в реестр.

| Проверка | Результат |
|---|---|
| Сборка и первый запуск | `tyuin-app-1` — `healthy`; порт VM привязан к `127.0.0.1:8765` |
| `moneygraph verify` | 2 248 узлов, 88 кластеров, топ-20; `analysis_id=ad6239d9995448b0` |
| Существующие тесты в отдельном контейнере без сети | **29 passed**, 7,77 с; одно прежнее предупреждение Starlette о TestClient/httpx |
| API через SSH-туннель | Health, анализ, досье и локальное объяснение отвечают; один `analysis_id` |
| Три CSV через API | HTTP 200; SHA-256 каждой выгрузки совпадает с файлом в `/app/out` |
| Браузер через SSH-туннель | Интерфейс загружен; поиск `100000000031787100` выбирает досье транзитного узла и его окружение; ошибок консоли нет |
| Перезапуск контейнера | Повторный `healthy`, успешные health и `verify`, тот же `analysis_id` |

### Подключение через SSH

Нужны вход в Brev и SSH-доступ к организации инстанса. На компьютере пользователя запустите в Bash/WSL и оставьте команду работающей:

```bash
brev refresh
brev port-forward tyuin --port 18765:8765
```

Откройте [http://127.0.0.1:18765](http://127.0.0.1:18765). Это локальный вход в туннель к облачному приложению, а не общая веб-ссылка. При проверке использован эквивалентный OpenSSH-туннель:

```bash
ssh -N -T -o ExitOnForwardFailure=yes -L 127.0.0.1:18765:127.0.0.1:8765 tyuin
```

Обычный SSH-шлюз Brev и передача архива через `scp ... tyuin:...` проверены. Прямое подключение `tyuin-host` с `--host` получало тайм-аут, поэтому для этой VM оно не используется. После перезагрузки компьютера туннель нужно открыть снова.

Для обслуживания текущей версии: `brev shell tyuin`, затем `cd ~/workspace/tyuin-f4077ff`, `export TYUIN_IMAGE=tyuin:f4077ff` и команды Compose из раздела «Управление и данные». Предыдущий каталог `~/workspace/tyuin-3c6b1cd` сохранён для отката и конфигурации Caddy. VM оставлена работающей. Остановка контейнера не прекращает оплату VM; для остановки вычислений используйте `brev stop tyuin` или Brev Console, учитывая отдельную плату за сохранённый диск.

**Review: принять выполненный облачный запуск в проверенном объёме.** Подтверждены воспроизводимость существующего приложения на Brev и доступ через SSH; алгоритмы и локальный запуск сохранены. Публичный доступ проверен отдельно ниже. Secure Link и внешний LLM не проверены. Слияние в `main` требует отдельной приёмки по правилам проекта.

## Собственный домен и HTTPS

**Текущий адрес: [https://hackalemai.uniqore.dev](https://hackalemai.uniqore.dev).** Домен направлен непосредственно на Brev. Пользователь выбрал собственный домен после первоначального запуска через ngrok. Вход в Brev посетителю не нужен; опубликован ранее согласованный объём демонстрационных данных, API и трёх CSV.

Первая проверка собственного домена 23.09.2026 выполнена на версии приложения `3c6b1cd`, алгоритм 1.0.0, `analysis_id=ad6239d9995448b0`. Конфигурация домена перенесена в ветку `codex/brev-public-domain` от `main` `6620aa1`. Результаты облачных проверок ниже относятся к этому commit и снимку анализа.

На VM добавлен отдельный контейнер Caddy 2.11.4 из официального образа, закреплённого по digest. Файлы [domain-web.compose.yml](../../deploy/brev/domain-web.compose.yml) и [Caddyfile](../../deploy/brev/caddy/Caddyfile) направляют запросы домена в приложение на `127.0.0.1:8765`. Входящие TCP 80/443 разрешены в UFW; порт приложения и административный API Caddy `127.0.0.1:2019` остаются на loopback. Контейнер использует сеть хоста Linux и автоматически запускается после перезапуска Docker.

Эти два файла переданы на VM отдельно из текущей конфигурации проекта с сохранением путей `deploy/brev/domain-web.compose.yml` и `deploy/brev/caddy/Caddyfile`. Они не входят в первоначальный архив commit `3c6b1cd`: этот SHA обозначает версию приложения. Для повторения настройки перенесите оба файла в развёрнутый каталог либо используйте ревизию репозитория, которая уже их содержит.

Сертификаты и ключи хранятся в Docker volume `tyuin_caddy-data`, состояние конфигурации — в `tyuin_caddy-config`. Эти тома сохраняются при пересоздании контейнера; ключи не находятся в Git или образе приложения. Caddy автоматически выпускает и продлевает сертификаты, HTTP перенаправляется на HTTPS. Будущее продление пока не наблюдалось; проверены первоначальный выпуск и сохранение сертификата после перезапуска.

### DNS и запуск

На дату проверки DNS Porkbun содержит запись A для имени `hackalemai` в зоне `uniqore.dev` со значением `81.85.2.195`, TTL 600. Запись подтверждена публичными резолверами и авторитетными серверами; AAAA и ограничивающие CAA отсутствуют. Это адрес текущей VM, а не обещание постоянного IP: после её остановки и повторного запуска проверьте IP и при необходимости обновите DNS.

Для уже работающего приложения из каталога `~/workspace/tyuin-3c6b1cd` в Bash на VM:

```bash
export TYUIN_DOMAIN=hackalemai.uniqore.dev

docker compose --env-file .env.example \
  -f docker-compose.yml -f deploy/brev/domain-web.compose.yml config --quiet
docker compose --env-file .env.example \
  -f docker-compose.yml -f deploy/brev/domain-web.compose.yml \
  run --rm --no-deps domain-web caddy validate --config /etc/caddy/Caddyfile --adapter caddyfile

sudo ufw allow proto tcp from any to any port 80 comment tyuin-domain-http
sudo ufw allow proto tcp from any to any port 443 comment tyuin-domain-https

docker compose --env-file .env.example \
  -f docker-compose.yml -f deploy/brev/domain-web.compose.yml up -d --no-deps domain-web
curl --fail https://hackalemai.uniqore.dev/api/health
```

`TYUIN_DOMAIN` обязателен; укажите только DNS-имя без схемы и пути. Если порт приложения изменён, задайте такой же `TYUIN_PORT` при запуске Caddy. При пересоздании контейнера переменные нужно задать снова; в работающем контейнере они уже сохранены. После изменения Caddyfile можно применить его без перезапуска:

```bash
docker compose --env-file .env.example \
  -f docker-compose.yml -f deploy/brev/domain-web.compose.yml \
  exec -T domain-web caddy reload --config /etc/caddy/Caddyfile --adapter caddyfile
```

Остановить только HTTPS-прокси: та же команда Compose с `stop domain-web`. Для возобновления используйте `up -d --no-deps domain-web`. Не удаляйте том `caddy-data` при обычном обновлении. Остановка VM прекращает доступ к сайту; Docker restart policy не включает остановленную VM.

### Проверка собственного домена

Проверено 23.09.2026 на неизменённом приложении `3c6b1cd`:

| Проверка | Результат |
|---|---|
| Конфигурация | Compose и Caddyfile валидны; без `TYUIN_DOMAIN` Compose отклоняет запуск |
| Внешний HTTPS | HTTP 200, обычная проверка доверия сертификату успешна, без `--insecure` |
| Сертификат | Let's Encrypt YE1, имя `hackalemai.uniqore.dev`; первоначальный срок до 22.12.2026 |
| HTTP | 308 на `https://hackalemai.uniqore.dev/` |
| Интерфейс | Загружены граф и логотип; поиск `100000000031787100` выбирает досье транзитного узла |
| API | Health, анализ, досье и локальное объяснение — HTTP 200; `analysis_id=ad6239d9995448b0` |
| Три CSV | HTTP 200; SHA-256 совпадают с ранее проверенными файлами приложения |
| Перезапуск Caddy | HTTPS health восстановился; SHA-256 отпечаток сертификата до/после одинаков |
| Переключение | После проверки нового домена `tyuin-public-web-1` остановлен; Tyuin остаётся `healthy` |

Прежние 29 тестов приложения относятся к неизменённому коду. Для этого изменения проверен новый путь доступа; полный набор тестов не повторялся. Тестовая проверка прямых портов до настройки домена описана ниже отдельно.

**Review: принять переход на собственный домен в проверенном объёме.** Интерфейс и результаты доступны по HTTPS без аккаунта команды или промежуточной страницы ngrok; самостоятельный запуск сохранён. Алгоритмы и качество аналитики этим изменением не затронуты. Доступность зависит от VM, актуальной DNS-записи и состояния Caddy; слияние в `main` по-прежнему требует отдельной приёмки.

## Публичная HTTPS-ссылка через ngrok (предыдущий вариант)

До перехода на собственный домен проверен адрес `https://superplausibly-unamortized-tomasa.ngrok-free.dev`. **Сейчас туннель остановлен; используйте [текущий адрес](https://hackalemai.uniqore.dev).** Ниже сохранены конфигурация и результаты предыдущей проверки. Домен закреплён за аккаунтом ngrok и явно задан при запуске. Пользователь подтвердил публичный доступ без авторизации к демонстрационным данным кейса: синтетическим `gid`, суммам и датам операций, ролям, кластерам и трём CSV.

Приложение остаётся на Brev. Дополнительный файл [public-web.compose.yml](../../deploy/brev/public-web.compose.yml) запускает ngrok Agent 3.39.8; официальный образ закреплён по digest. Туннель направлен на `http://127.0.0.1:8765` хоста VM, исходный контейнер приложения не изменён. `restart: unless-stopped` восстанавливает туннель после завершения процесса или перезапуска Docker, с тем же указанным доменом.

### Настройка и управление

На VM конфигурация ngrok хранится вне checkout, например `~/.config/tyuin/ngrok.yml`, с правами `0600` и владельцем UID 1000. Файл монтируется только для чтения в контейнер туннеля, работающий как UID/GID 1000. Его структура:

```yaml
version: "3"
agent:
  authtoken: <NGROK_AUTHTOKEN>
  web_addr: 127.0.0.1:4040
```

Замените placeholder в приватном файле токеном своего аккаунта; не сохраняйте его в репозитории. Домен должен принадлежать тому же аккаунту. Локальный API агента доступен только на loopback VM, HTTP inspection отключён через `--inspect=false`.

Из корня развёрнутого приложения в Bash на VM:

```bash
export TYUIN_NGROK_CONFIG="$HOME/.config/tyuin/ngrok.yml"
export TYUIN_NGROK_URL="https://superplausibly-unamortized-tomasa.ngrok-free.dev"

docker compose --env-file .env.example \
  -f docker-compose.yml -f deploy/brev/public-web.compose.yml config --quiet
docker compose --env-file .env.example \
  -f docker-compose.yml -f deploy/brev/public-web.compose.yml up -d public-web
```

Для повторного создания контейнера снова задайте обе переменные в текущем терминале. В существующем контейнере адрес уже сохранён в аргументах запуска. Не выводите полный `config` с реальными секретами; для проверки используйте `config --quiet`.

После тех же `export` остановить доступ через ngrok, сохранив приложение, собственный домен Caddy и SSH-доступ:

```bash
docker compose --env-file .env.example \
  -f docker-compose.yml -f deploy/brev/public-web.compose.yml stop public-web
```

Для возобновления выполните команду `up -d public-web` выше. Остановка или удаление VM делает веб-ссылку недоступной; сам домен остаётся привязан к аккаунту ngrok.

### Проверка и ограничения

Проверено 23.09.2026 на той же версии приложения `3c6b1cd` с добавленным Compose-файлом туннеля:

| Проверка | Результат |
|---|---|
| Проверка Compose | Успешна; без `TYUIN_NGROK_URL` запуск отклоняется с понятной ошибкой |
| Публичный HTTPS без учётных данных Brev | Интерфейс загружен, `/api/health` — `ok`; `analysis_id=ad6239d9995448b0` |
| Анализ, досье и объяснение | 2 248 узлов, 88 кластеров, досье выбранного клиента и локальное объяснение относятся к одному снимку |
| Три публичных CSV | HTTP 200; SHA-256 совпадают с файлами на VM и ранее проверенными выгрузками через SSH |
| Перезапуск `tyuin-public-web-1` | Закреплённый домен сохранён; публичный health снова отвечает |

Прежние 29 тестов приложения на VM относятся к неизменённому коду. Для этого изменения проверены публичный маршрут и конфигурация туннеля; повторный запуск всего набора тестов не выполнялся.

На бесплатном тарифе ngrok перед HTML может появляться страница с кнопкой **Visit Site**. По документации на дату проверки тариф ограничен 1 ГБ исходящего трафика и 20 000 HTTP-запросов в месяц. Это демонстрационный доступ с зависимостью от доступности VM, агента и лимитов ngrok, без гарантии постоянной доступности. Приложение по-прежнему можно запустить локально без Brev и ngrok.

**Review: принять публичный доступ в проверенном объёме.** Появился URL для демонстрации без аккаунтов команды; результаты анализа и самостоятельный запуск сохранены. Токен не включён в репозиторий или образ приложения. Авторизация посетителей отсутствует по подтверждённому запросу пользователя; адрес сохраняется благодаря явному `--url`.

## Проверка прямого доступа без туннеля

23.09.2026 по запросу пользователя проверен прямой входящий доступ к VM `tyuin` для подключения собственного домена через DNS Porkbun. На момент проверки публичный адрес VM — `81.85.2.195`. Этот адрес подтвердился внешними запросами, а не только проверкой исходящего IP.

Для проверки использован отдельный временный HTTP/HTTPS-ответчик на портах 80 и 443. Он возвращал только фиксированный тестовый маркер; приложение и его файлы не обслуживал. До разрешения портов в UFW установлен серверный таймер очистки через пять минут. Для TLS создан одноразовый самоподписанный сертификат.

| Проверка | Результат |
|---|---|
| Внешний HTTP к `81.85.2.195:80` | HTTP 200, получен ожидаемый тестовый маркер |
| Внешний HTTPS к `81.85.2.195:443` | TLS-соединение и HTTP 200, получен тот же маркер; проверка доверия сертификату отключена только для этого теста |
| Наблюдение на VM | Зафиксированы входящие TCP SYN на оба порта |
| Очистка | Тестовый сервис и сбор заголовков остановлены, добавленные IPv4/IPv6-правила удалены, исходный вывод `ufw status` восстановлен полностью; таймер остановлен, одноразовый сертификат и ключ удалены |
| Приложение после проверки | `healthy`, `/api/health` — `ok`, прежний `analysis_id=ad6239d9995448b0`; порты 80/443 снова свободны |

**Вывод на момент проверки: прямое подключение собственного домена без Cloudflare и ngrok технически возможно на проверенной VM.** На этом этапе A-запись, Caddy и публично доверенный сертификат ещё не были настроены. Результат последующего подключения приведён в разделе [«Собственный домен и HTTPS»](#собственный-домен-и-https). После остановки и повторного запуска VM IP может измениться; тогда DNS потребуется обновить.

**Review: принять результат проверки доступности.** Подтверждён прямой маршрут HTTP/HTTPS; эта предварительная проверка сама по себе не подтверждала собственный домен и доверенный сертификат. Код приложения и данные не изменялись, повторный прогон тестов приложения не требовался.

## Источники

Формат и настройки сверены с официальной документацией NVIDIA, Caddy и ngrok:

- [Custom Docker Containers](https://docs.nvidia.com/brev/guides/development-tools/custom-containers): Compose upload/URL и режим VM.
- [Launchables](https://docs.nvidia.com/brev/concepts/launchables): Launch parameters, Source, Secure Link и время жизни параметров.
- [CLI Connectivity](https://docs.nvidia.com/brev/cli/connectivity): `brev shell`, `--host`, port forwarding и ограничения веб-туннелей.
- [ngrok Docker](https://ngrok.com/docs/using-ngrok-with/docker/): запуск отдельного контейнера и закрепление URL.
- [ngrok Free Plan Limits](https://ngrok.com/docs/pricing-limits/free-plan-limits): домены, лимиты и промежуточная страница.
- [Caddy Docker Official Image](https://hub.docker.com/_/caddy): образ, конфигурация и постоянное хранение сертификатов.
- [Caddy Automatic HTTPS](https://caddyserver.com/docs/automatic-https): выпуск и продление сертификатов, DNS и входящие порты.
